name: Build

on:
  push:
    paths:
      - "config/**"
      - "build.yml"
  pull_request:
    paths:
      - "config/**"
      - "build.yml"
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
